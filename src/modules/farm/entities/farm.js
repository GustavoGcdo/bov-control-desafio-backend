export class Farm {
  constructor({ code, name, distanceFromFactory, farmers }) {
    this.code = code;
    this.name = name;
    this.distanceFromFactory = distanceFromFactory;
    this.farmers = farmers;
  }
}
